version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      FLASK_ENV: production
      FLASK_APP: run.py
      PORT: 5000
    ports:
      - "5001:5000"   # single container, one port (keep old backend external port)
    restart: unless-stopped
