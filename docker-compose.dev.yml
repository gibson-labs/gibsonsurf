version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      FLASK_ENV: development
      FLASK_APP: run.py
      PORT: 5000
    ports:
      - "5002:5000"
    restart: unless-stopped
